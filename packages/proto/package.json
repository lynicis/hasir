{
  "name": "@hasir/proto",
  "private": true,
  "dependencies": {
    "@bufbuild/protobuf": "^2.15.0"
  },
  "scripts": {
    "proto": "buf generate",
    "lint": "buf lint",
    "format": "buf format -w"
  }
}
