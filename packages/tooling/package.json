{
  "name": "@hasir/tooling",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "hasir-proto-gen": "./bin/proto-gen.mjs",
    "hasir-release": "./bin/release.mjs"
  },
  "scripts": {
    "build": "bun build bin/proto-gen.mjs bin/release.mjs --outdir dist --target bun",
    "lint": "eslint bin",
    "clean": "rimraf dist .tsbuildinfo"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14"
  }
}
